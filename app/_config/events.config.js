/**
 * Custom window events used to talk between the navbar and the offcanvas menu,
 * which sit in separate React trees (the menu is mounted by the root layout).
 */
export const OPEN_MENU_EVENT = 'portfolio-open-menu';
